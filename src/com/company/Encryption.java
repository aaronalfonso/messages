package com.company;

/**
 * Created by aa700 on 10/16/17.
 */
public class Encryption {
    private double Dkey = Math.random() * (Math.random() * 10);
    private int key = (int)Dkey;

    public Encryption(String word) {
        String cipher = word;
        int length = word.length();
        for (int i=0; i<length; i++) {

        }

    }
}
